name: build-push-ecr-agent

on:
  workflow_dispatch:
  push:
    branches: [master]
    paths:
      - "agent/**"
      - ".github/workflows/build-push-ecr-agent.yml"

env:
  AWS_REGION: ca-central-1
  ECR_REPOSITORY: aiops-agentcore-agent

permissions:
  id-token: write # required for OIDC
  contents: read

concurrency:
  group: build-push-ecr-agent
  cancel-in-progress: false

jobs:
  unit-test:
    name: pytest
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: agent/requirements-dev.txt

      - name: Install dependencies
        run: pip install -r agent/requirements-dev.txt

      - name: Run agent tests
        working-directory: agent
        run: pytest

  build-push:
    name: build and push
    needs: unit-test # build push only when unit test get passed
    # ARM64 runner: AgentCore Runtime requires linux/arm64, and building
    # natively avoids the slow QEMU emulation a cross-build would need.
    runs-on: ubuntu-24.04-arm
    steps:
      - uses: actions/checkout@v7

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: ${{ vars.AWS_ROLE_ARN_ECR_OIDC }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Log in to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build and push
        uses: docker/build-push-action@v7
        with:
          context: agent
          platforms: linux/arm64 # required by the AgentCore Runtime
          push: true
          provenance: false
          tags: |
            ${{ steps.login-ecr.outputs.registry }}/${{ env.ECR_REPOSITORY }}:latest
            ${{ steps.login-ecr.outputs.registry }}/${{ env.ECR_REPOSITORY }}:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Summarise
        run: |
          echo "Pushed \`${ECR_REPOSITORY}:latest\` and \`${ECR_REPOSITORY}:${GITHUB_SHA}\`" >> "$GITHUB_STEP_SUMMARY"
